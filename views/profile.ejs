<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon Profil</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
<link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <!-- Sidebar -->

<%- include('partials/sidebar', { user: user }) %>



<div class="profile-page">
  <h1>Bienvenue, <%= user.name %></h1>
  <img src="/images/<%= user.image %>" alt="Photo de profil de <%= user.name %>">

  <div class="profile-info">
    <p><strong>Nom complet :</strong> <%= user.name %></p>
    <p><strong>Email :</strong> <%= user.email %></p>
    <p><strong>Mot de passe :</strong> *********</p> <!-- Ne jamais afficher le vrai mdp ! -->
    <p><strong>Status :</strong> 
      <span class="<%= user.is_online ? 'status-online' : 'status-offline' %>">
        <%= user.is_online ? 'En ligne' : 'Hors ligne' %>
      </span>
    </p>
  </div>

  <div class="profile-links">
    <a href="/dashboard">Retour au dashboard</a> |
    <a href="/logout">Déconnexion</a>
  </div>

  <div class="edit-button-container">
    <a href="/profile/edit" class="btn-edit">Éditer mon profil</a>
  </div>
</div>
